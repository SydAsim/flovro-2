Earth surface textures are sourced from the MIT-licensed `three-globe` example
assets. The maps originate from public Earth observation imagery and are bundled
locally so the hero does not depend on a third-party image request at runtime.
